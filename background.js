// Service worker for URL Environment Switcher
// Currently minimal — placeholder for future features (context menu, keyboard shortcuts).

chrome.runtime.onInstalled.addListener(() => {
  console.log("URL Environment Switcher installed");
});
